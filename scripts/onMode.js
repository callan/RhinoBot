// include basicInformation.js

function onMode (channel, modes, nick, ident, hostmask, permission, bot)
{
//	for (var i = 0; i < modes.size(); i++)
//	{
//		var mode = modes.get(i);
//		bot.privmsg(channel, "Mode: " + ((mode.isPlusMode()) ? '+' : '-') + mode.getModeAsString());
//		bot.privmsg(channel, "Param: " + ((mode.getParameter() != "") ? mode.getParameter() : "none" ));
//	}
}
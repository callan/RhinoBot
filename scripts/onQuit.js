// include basicInformation.js

function onQuit (reason, nick, ident, hostmask, permission, bot)
{
/*
	var manager = bot.getManager();
	var user    = manager.getUser(nick);
	
	if (user != null)
	{
		var channels = user.getChannels();
		
		for (var i = 0; i < channels.length; i++)
		{
			if (bot.inChannel(channels[i]))
			{
				bot.privmsg(channels[i], "It appears " + nick + " has left the network :<");
			}
		}
	} */
}
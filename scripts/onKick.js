// include basicInformation.js

function onKick (channel, kicked, reason, nick, ident, hostmask, permission, bot)
{
/*	if (kicked != botNick)
	{
		bot.privmsg(channel, "User " + kicked + " was kicked by " + nick + " because: " + reason);
	}
	else
	{
		bot.notice(nick, "Why did you kick me? :(");
	} */
}
// include basicInformation.js

function onNick (newNick, nick, ident, hostmask, permission, bot)
{
//	bot.notice(newNick, "You just changed from " + nick + " to " + newNick + ", didn't you?");
}